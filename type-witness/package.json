{
  "name": "type-witness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A browser-based TypeScript type-inference visualizer: paste a snippet and watch the compiler think, expression by expression. Literal types, widening, generic inference, control-flow narrowing, and the exact step where an error appears — all computed by the real TypeScript compiler running in a Web Worker. Fully client-side: no API keys, no accounts, nothing leaves your browser.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  },
  "dependencies": {
    "typescript": "5.6.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript-eslint": "^8.19.0",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
