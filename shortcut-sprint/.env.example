# Shortcut Sprint needs no environment variables.
# The app is fully client-side: no backend, no API keys, no analytics.
# This file exists to match the repo convention that every package documents
# its runtime configuration.
