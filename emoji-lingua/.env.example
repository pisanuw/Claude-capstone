# emoji-lingua environment. The app works fully with NONE of these set:
# without a key it uses the built-in dictionary translator.
PORT=3000
# Optional: enables smarter, context-aware translation via Claude.
ANTHROPIC_API_KEY=
# Optional: override the model (defaults to claude-sonnet-4-20250514).
ANTHROPIC_MODEL=
