{
  "name": "prompt-genome",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A visual prompt-engineering workbench that splits any pasted prompt into typed, color-coded genes (role, context, task, constraint, format, example, persona), lints the genome for weaknesses, offers three deterministic mutations per gene, and diffs original vs edited prompt word by word. Fully client-side: no API keys, no accounts, nothing leaves your browser.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.19.0",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
