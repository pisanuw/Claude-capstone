# Deploy control file for Prompt Genome (Netlify, static site).
#
# No backend and no environment variables: segmentation, lint, mutations,
# diff, the localStorage library, and share-link encoding are all
# deterministic client-side logic. The deploy workflow auto-discovers this
# file via the `*/deploy/target.yml` glob.

provider: netlify
projectName: prompt-genome
directory: prompt-genome
build: npm ci && npm run build
publish: dist

# No secrets or runtime configuration are needed.
# (Touch this file to retrigger a deploy: the workflow deploys changed target.ymls.)
envPassthrough: []

netlify:
  # No serverless functions; the whole app is static.
  functionsDir: ''
  siteName: prompt-genome
  accountSlug: yusuf-pisan
