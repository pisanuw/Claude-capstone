{
  "name": "cron-cartographer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Renders any cron expression, RRULE, or GitHub Actions schedule as an interactive calendar heatmap showing exactly when jobs fire over the next 30, 90, or 365 days, with timezone-aware conversion, plain-English schedule descriptions, and a deterministic natural-language-to-cron converter. Fully client-side: no API keys, no accounts, nothing leaves your browser.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.69.0",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
