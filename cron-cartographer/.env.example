# Cron Cartographer needs no environment variables.
#
# Everything (cron/RRULE/GitHub Actions parsing, natural-language conversion,
# time-zone math, heatmap rendering, share links) runs client-side with no
# API keys and no backend. This file exists to make that explicit.
