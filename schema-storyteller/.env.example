# Schema Storyteller has no runtime configuration.
#
# The entire app is static and runs in the browser: parsing, narration, and the
# rule-based review are all deterministic client-side logic. There is no server,
# no database, and no external API, so there are no secrets or environment
# variables to set. This file exists only to document that fact and to match the
# repo's project layout convention.
