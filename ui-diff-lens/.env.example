# ui-diff-lens needs no environment variables.
#
# The whole app is static and runs in the browser: the pixel diff, region
# clustering, and change classification are deterministic client-side logic.
# Screenshots never leave the machine. This file exists to document that fact.
