# SQL Replay has no environment variables.
#
# Everything — SQL parsing, staged execution, narration, CSV handling, and
# share links — is deterministic client-side code. There is no backend, no
# API key, and no telemetry. This file exists to make that explicit and to
# match the layout of the other packages in this monorepo.
