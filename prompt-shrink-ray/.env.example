# Prompt Shrink Ray has no environment variables.
#
# Decomposition, compression, token/cost estimation, and the retention check
# are all deterministic client-side logic; there are no API keys and no
# backend. This file exists to keep the package layout consistent across the
# monorepo.
