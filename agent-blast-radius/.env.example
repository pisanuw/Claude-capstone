# Agent Blast Radius has no environment variables.
#
# Parsing, capability scoring, and report generation are all deterministic
# client-side logic; there are no API keys and no backend. This file exists
# to keep the package layout consistent across the monorepo.
