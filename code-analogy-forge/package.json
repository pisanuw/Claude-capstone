{
  "name": "code-analogy-forge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Paste code or name a CS concept and get three sharply framed analogies from different everyday domains, calibrated to your audience: curious child, high school student, CS undergraduate, or non-technical adult. A hand-written concept detector and curated analogy corpus make it deterministic, offline, and free; no API keys, no accounts.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.19.0",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
