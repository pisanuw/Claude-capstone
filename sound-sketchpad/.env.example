# Sound Sketchpad has no environment variables.
#
# Everything — description parsing, synthesis, playback, and WAV export — is
# deterministic client-side code. There is no backend, no API key, and no
# telemetry. This file exists to make that explicit and to match the layout
# of the other packages in this monorepo.
