# Migration Diff Narrator has no environment variables.
#
# Parsing, diffing, and the severity/narration rules are all deterministic
# client-side logic; there are no API keys and no backend. This file exists to
# keep the package layout consistent across the monorepo.
