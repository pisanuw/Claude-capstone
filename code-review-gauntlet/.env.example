# Code Review Gauntlet needs no environment variables.
# The app is fully client-side: no backend, no API keys, no analytics.
# Puzzles come from a seeded deterministic mutation engine, and all progress
# lives in localStorage. This file exists to match the repo convention that
# every package documents its runtime configuration.
