# har-detective needs no environment variables.
#
# The whole app is static and runs in the browser: HAR parsing, the waterfall
# renderer, and every performance detector are deterministic client-side
# logic. HAR files never leave the machine. This file exists to document that
# fact.
